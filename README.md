# create-lazy-init

A project initialization tool built for developers who are tired of looking up documentation every time they start a new repository. Instead of wasting time trying to remember the basic setup steps, this CLI handles the boilerplate so you can get straight to coding. 

Under the hood, it leverages Vite's official templates for fast, reliable scaffolding.